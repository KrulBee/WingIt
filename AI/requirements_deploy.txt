# Deployment Requirements for Render/Cloud Platforms
# Optimized for cloud deployment

# Core PyTorch (regular version - works on all platforms)
torch==2.5.1
transformers==4.35.0

# Web Framework
flask==2.3.3
flask-cors==4.0.0

# Essential dependencies
numpy==1.24.3
requests==2.31.0

# Tokenizers (compatible with transformers 4.35.0)
tokenizers>=0.14,<0.15
