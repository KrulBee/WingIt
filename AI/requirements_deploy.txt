# Deployment Requirements for Render/Cloud Platforms
# Optimized for cloud deployment (smaller, faster installs)

# Core PyTorch (CPU-only for cloud deployment)
torch==2.5.1+cpu --find-links https://download.pytorch.org/whl/torch_stable.html
transformers==4.35.0

# Web Framework
flask==2.3.3
flask-cors==4.0.0

# Essential dependencies
numpy==1.24.3
requests==2.31.0

# Optional: Reduce memory usage
# --no-deps  # Uncomment to skip automatic dependency resolution
